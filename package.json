{
  "name": "verjaardaglauren",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel serve ./src/index.html --open",
    "build": "npm run clean && parcel build ./src/index.html --dist-dir ./out --no-source-maps",
    "clean": "rm -rf ./.parcel-cache ./dist ./out",
    "serve": "live-server ./out",
    "deploy": "surge ./out --domain https://verjaardaglauren.surge.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.0.1",
    "@parcel/transformer-sass": "^2.0.1",
    "@parcel/transformer-webmanifest": "^2.0.1",
    "parcel": "^2.0.1",
    "posthtml-include": "^1.7.2"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^1.7.1",
    "bulma": "^0.9.3",
    "redux-logger": "^3.0.6"
  }
}
